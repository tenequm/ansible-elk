server {
  listen              80;
  listen              [::]:80;
  server_name         {{ elk_server_domain }};

  location /.well-known {
    root       /var/www/html;
  }

  location / {
    return              301 https://$server_name$request_uri;
  }
}
